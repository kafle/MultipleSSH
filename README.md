Connect to multiple ssh servers and run a command on each using threads.

- Uses pxssh
- Reads from a file specified with '-H' with '<user>:<passwd>@<hostname>' on each line
- Slightly formatted output
